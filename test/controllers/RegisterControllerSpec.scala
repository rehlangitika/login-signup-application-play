/*package controllers

import org.scalatest.mock.MockitoSugar
import org.scalatestplus.play.{OneAppPerTest, PlaySpec}
import org.mockito.Mockito._
import play.api.test.FakeRequest
import play.api.http.Status._
import play.api.test.Helpers.{GET, contentAsString, contentType, route, status}
import services.UserService
/**
  * Created by gitika on 9/3/17.
  */
class RegisterControllerSpec extends PlaySpec with MockitoSugar {

val cacheDataService = mock(classof[UserService])
  /*"RegisterController" should {
    "render the signup page" in {
      val signup = route(app, FakeRequest(GET, "/")).get
      status(signup) mustBe OK
      contentType(signup) mustBe Some("text/html")
      contentAsString(signup) must include("Sign Up")
    }
  }

  "RegisterController" should {
    "render the profile page" in {
      val profile = route(app, FakeRequest(GET, "/")).get
      status(profile) equals 303
      contentType(profile) mustBe Some("text/html")
      contentAsString(profile) must include("Your Profile")
    }
  }*/

  "Register Controller" should {
    "render the profile page" in {

    }
  }

}*/
